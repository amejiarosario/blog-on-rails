%h1 Blog#index
%p Find me in app/views/blog/index.html.haml

- @posts.each do |post|
	%h3= link_to post.title, post
	%p= "by #{post.name} in #{post.created_at}"
	%p= post.content
	%p=raw "tags: " + post_tags(post)
	%p= "#{post.comments.length} comments."
	%hr